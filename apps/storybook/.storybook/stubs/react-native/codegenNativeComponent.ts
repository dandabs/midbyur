// Stub for react-native/Libraries/Utilities/codegenNativeComponent
// This is a native-only codegen utility. In Storybook/web contexts it is never
// exercised, so we return a no-op component factory to satisfy the import.
export default function codegenNativeComponent(_name: string): unknown {
  return null;
}
