// Tailwind CSS is handled by the @tailwindcss/vite plugin in .storybook/main.ts.
// PostCSS is kept for any other transforms but Tailwind is excluded here
// to avoid double-processing.
const config = {
  plugins: {},
};

export default config;
